# TODO

## SU, SUDO, root

- [X] Проверить доступность `root` прав для создания маршрутов и поднятия *web* сервера;

## Исправить функции

- [ ] В IPv6 метрика `0` интерпретируется ядром как *использовать значение по умолчанию*. А значение по умолчанию для статических маршрутов в IPv6 - это `1024`.

`show_routes`

- [X] Если `show_routes` использует `netstat`, тогда мы не получим метрику у выводимых маршрутов. Возможно, что раз `show_routes` использует `netstat`, значит система не поддерживает установку маршрутов с метрикой. Нужно проверить логику определения `show_routes`, возможно `route` умеет показывать маршруты, и тогда лучше его задействовать.

`control_route`/`evaluate_speed`/`evaluate_host`

- [X] Принять на обработку отрицательный код возврата комад.

`resolve_dependencies`

- [X] Поправить код `awk` в переменных `AWK_UNIQUE_COLLECT` и/или `AWK_NATURAL_SORT`, т.е. есть мусор:

      ```
      root@OpenWrt:~# show_ports
      netstat: no kernel support for AF INET6 (tcp)
      netstat: no kernel support for AF INET6 (udp)
      netstat: no kernel support for AF INET6 (raw)
      root@OpenWrt:~#
      ```

`verify_network_state`

- [ ] Добавить проверку поддержки IPv[46] через параметры ядра и по возможности другие средства

- [ ] Проверить интерфейс `lo` на состояния UP/DOWN

`setup_core_env`

- [X] Определить программу для формирования символа TAB и CR

`bit2Human`

- [X] Заменить `printf` на что-то вроде:

      ```sh
      if test "$REMAINS" -lt 10
      then
          REMAINS=".0$REMAINS"
      else
          REMAINS=".$REMAINS"
      fi
      # или
      is_diff ${#REMAINS} 1 && REMAINS=".$REMAINS" || REMAINS=".0$REMAINS"
      ```

`get_obsolete_routes`

- [X] Заменить `printf` на `echo` или `<<EOF`

`count_metrics`

- [X] Избавиться от `awk`. Количество можно получить используя `set -- $METRICS` которые можно заведомо собирать.

`parse_resource`

- [X] Проверить наличие *www*

- [X] Испраить определение маски сети, http://example.com/24 - сейчас 24 попадает под маску, а должно быть ресурсом на хосте.

- [X] Проверить переменные, сейчас ://example.com/24 - SCHEME пуста и мы не сообщаем об ошибке.

`say`

- [X] Проверить программу `echo` на интерпритацию бэкслеши (`\n`, `\t`), и флаг `-e`

- [X] Добавить ключ который не будет перезаписывать переменную `RESULT`

`show_ports`

- [ ] Добавить ключ для `-t` и `-u` для tcp и udp портов.

`is_port_free`

- [X] 1. Избавиться от `cat`

- [X] 2. Избавиться от Linux-isms.

- [X] 3. Взависимости от решения `2.`, заменить `printf` переносом его обязанностей в `awk`:

         ```sh
         cat /proc/net/tcp /proc/net/tcp6 2>/dev/null | awk -v port="$1" '
             BEGIN {
                 # Формируем паттерн HEX-порта: :0050 (для 80 порта)
                 # В конце добавляем $, чтобы исключить частичные совпадения
                 hex_port = sprintf(":%04X$", port)
             }
             $2 ~ hex_port {
                 found = 1
                 exit
             }
             END {
                 exit (found == 1 ? 1 : 0)
             }
         '
         ```

`remove_test_route`

- [X] 1. Сначала найти все маршруты для ${PING_IPV4:-} ${SPEEDTEST_IPV4:-} ${PING_IPV6:-} ${SPEEDTEST_IPV6:-};

- [X] 2. Последовательно удалять и если не удалятся, выйти `return $RETURN`;

- [X] 3. Перевести логику на универсальные функции `show_routes` для **UNIX** и **GNU/Linux**;

`refresh_routing_table`

- [X] Должен обновлять как IPv4, так и IPv6 маршруты

`save_gateways_state`

- [X] При использовании `nc` как сервера, должно быть пропущено создание файла для раздачи сервером, т.к. в `nc` передаеться переменная, а не файл

`resolve_transfer_tools`

- [X] 1. Необходимо избавиться от поврторного поиска *web* клиента для *slave* режима, если уже найден *web* клиент для `speedtest`.

- [X] 2. Создать универсальные *web* функции на базе `wget`, `curl`, `nc`, ... для вызова в `speedtest` и для приема списка шлюзов с **master**.

- [ ] 3. Реализовать поддержку клиента и/или сервера: `socat`, `ncat`, `bash`, `Inetd`, `Xinetd`, `httpd`, ...

         > По возможности без использования файлов, т.е. передавать данные через переменные.
         >
         > Необходимо для систем не имеющих возможности записи в файловую систему.

         - [X] `uhttpd`

         - [X] `httpd`

         - [X] `telnetd`

         - [X] `netcat` (`nc`)

         - [X] `fetch` (***BSD***)

         - [ ] `socat`

         - [ ] `ncat`

         - [ ] `bash`

         - [ ] `inetd`

         - [ ] `xinetd`

         - [ ] `nginx`

         - [ ] `apache`

         - [ ] `php`

         - [ ] `python` ;(

- [X] 4. Выполнить проверку *web* клиента и сервера на поддержку IPv6 и `https://`

- [X] 5. В режиме **master/slave**, при определении *web* сервера, отдавать предпочтение серверу не требующему доступ к файловой системе, т.е. способному отдавать строку из переменной (`nc`), а не из файла.

- [X] 6. В режиме **master/slave**, при определении *web* сервера требующего работы с фаловой системой (`uhttpd`, `telnetd`, ...), необходимо проверить возможность записи переменно `GATEWAYS_STATE_FILE`

- [ ] 7. Для проверки `serve_gateways` на поддержку IPv[46] делаеться пробный запуск на 127.0.0.1/::1 с портом 1 для анализа ошибок.

         Необходимо ввести ключь/параметр для включения/выключения данной проверки.

`resolve_server` / `resolve_client`

- [X] Добавить сообщение о начале проверки для всех заявленных приложений, а не только для найденных.

`verify_config_syntax`

- [X] Сформировать SPEEDTEST_URL

`main` / `stop_serve_gateways`

- [X] При переключении из **master** на **slave** нужно остановить *web* сервер;

`check_ping` / `speedtest`

- [ ] Внедрить проверку количеством успешных и неудачных попыток:

      - `FALL=3`: Нужно 3 ошибки для признания сбоя
      - `RISE=2`: Нужно 2 успеха для восстановления

`fetch_gateways`

- [X] Реализовать поддержку IPv6:

## Конфигурация

- [X] 1. Добавить установку режима через параметр, сейчас хост переключаеться между режимами **mater** / **slave** если `VIRTUAL_IPADDRESS` установлен.

- [ ] 2. Для безопасности, необходимо избавиться от команды `source` (`.`) загружающей конфигурацию. Нужно построчное чтение и анализ аргументов.

- [ ] 3. Реализовать передачу параметров через аргументы (что позволит запускать с разными конфигурационными файлами)

- [ ] 4. Добавить возможность приминения конфигурации на лету по команде, например `reload`

- [ ] 5. Добавить не документированные переменные для работы `speedtest` и `check_ping`: `SPEEDTEST_TIMEOUT`, `PING_TIMEOUT`, `PING_COUNT`

- [ ] 6. Добавить не документированные переменные для работы `control_route(route)`: `ROUTE_FLAG_METRIC`, `ROUTE_FLAG_IPV4`, `ROUTE_FLAG_IPV6`

## Режим работы

**master-advisor**

- [X] Добавить возможность не устанавливать маршруты, а только определять и раздавать.

**master** | **master-advisor**

- [X] При отсутствии живых шлюзов отдавать пустой список, что бы **slave-survivor** не перешли в режим **autonomous**

**slave**

- [X] 1. Если **master** не доступен, должен перейти в **single** режим
         (на тот случай, если на **master** остановлен `kg`).
         По возвращению **master** вернуться в **slave**;

- [X] 2. **master** и **slave** могут иметь разные OS и соответственно разные имена сетевых интерфейсов. Нужно реализовать на **slave** подстановку собственных интерфейсов под полученные шлюзы от **master**.

**Interactive**

- [ ] Добавить цвет сообщениям если вывод на терминал

## Debug

- [ ] 1. Упростить определение ошибок и завершение функций. Сейчас давольно запутанно с двумя переменными EXIT_CODE и RETURN.

- [ ] 2. Проверить ключи у всех вызываемых програм на поддержку в разных ос и при необходимости найти универсальное решение в случаии несовместимости.

## Style / Perform

В awk коде:

  - [ ] прибрать `else { if ...` и `else if ...`

  - [ ] экранировать `.` в IP адресах

- [ ] Укоротить сообщения

- [X] Удалить избыточные конструкции `${VAR:-}`, они нужны, если переменные не инициализированы.

- [X] Удалить избыточные кавычки, пример `is_equal "$VAR" "yes"` > `is_equal "$VAR" yes`, а также в присвоении значения переменным `VAR=""` > `VAR=`

- [X] Везде где используеться `awk` привести его код к единообразному многострочному стилю

- [X] Обнаружено, что некоторые shell не правильно работают с вложенными кавычками при подстановке:

      ```sh
      C=45
      A=" "
      echo "${C:+$A$C}"   - результат: ' 45'
      echo "${C:+"$A$C"}" - результат: '45'
      echo "${C:+" $C"}"  - результат: 'bad substitution'
      echo  ${C:+" $C"}   - результат: ' 45'
      ```

      Решение, удалить вложенные кавычки.

- [X] Проверить необходимость кавычек VAR="$(...)" и VAR=$(...)

- [X] Проверить необходимость кавычек в операторе `case .. in *pattern*)` *pattern* должно быть в кавычках - `*"pattern"*)`.

- [X] Снизить количество перенаправлнии потоков, например `>/dev/null`

- [X] Проверить пустве строки

## Создать функции

- [X] Проврка PORT на допустимый диаппазон `[ "$1" -ge 1 ] && [ "$1" -le 65535 ]`

- [X] Реализовать замену `ip` на `ifconfig` и другие при отсутствии в системе (FreeBSD, ...)

- [ ] Проверить при запуске, не запущена ли уже другая копия программы

- [ ] Рассмотреть вариант с неудалением мертвого маршрута, а повышение его метрики `WEIGHT=50`

      > Рассматривать в последнюю очередь.
