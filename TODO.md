# TODO

## SU, SUDO

- [ ] Проверить доступность `root` прав для создания маршрутов и поднятия *web* сервера;

## Исправить функции

`remove_test_route`

- [ ] 1. Сначала найти все маршруты для ${PING_IPV4:-} ${SPEEDTEST_IPV4:-} ${PING_IPV6:-} ${SPEEDTEST_IPV6:-};

- [ ] 2. Последовательно удалять и если не удалятся, выйти `return $RETURN`;

`update_gateways_state`

- [ ] 1. При использовании `nc` должна быть пустой, т.к. в `nc` передаеться переменная, а не файл;

`stop_share_gateways`

- [X] 1. При переключении из **master** на **slave** нужно остановить *web* сервер;

`check_ping`

- [ ] 1. Внедрить проверку количеством успешных и неудачных попыток:

         - `FALL=3`: Нужно 3 ошибки для признания сбоя
         - `RISE=2`: Нужно 2 успеха для восстановления

## Создать функции

- [ ] Необходимо избавиться от поврторного поиска *web* клиента для *slave* режима, если уже найден *web* клиент для `speedtest`.

- [ ] Создать универсальные *web* функции на базе `wget`, `curl`, `nc`, ... для вызова в `speedtest` и для приема списка шлюзов с **master**.

- [ ] Реализовать поддержку клиента и/или сервера: `socat`, `ncat`, `bash`, `Inetd`, `Xinetd`, `httpd`, ...

      > По возможности без использования файлов, т.е. передавать данные через переменные.
      >
      > Необходимо для систем не имеющих возможности записи в файловую систему.

- [ ] Выполнить проверку *web* клиента и сервера на поддержку IPv6 и `https://`

- [ ] Рассмотреть вариант с неудалением мертвого маршрута, я повышение его метрики `WEIGHT=50`

      > Рассматривать в последнюю очередь.

## Режим работы

**slave**

- [ ] 1. Если **master** не доступен, должен перейти в **single** режим
         (на тот случай, если на **master** остановлен `kg`).
         По возвращению **master** вернуться в **slave**;
